import Header from "../../components/header/header";
import Body from "./receiptBody";

const Receipt = (props) => {
  return (
    <div>
      <Header />
      <Body />
    </div>
  );
};

export default Receipt;
